# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "tailrocks/ruxel"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-deny-policy", "cargo-lockfile", "mise-present", "renovate-configuration", "rust-binaries:ruxel-agent:1", "rust-binaries:ruxel-cli:1", "rust-build-script:ruxel-cli", "rust-build-script:ruxel-proto", "rust-test-targets:ruxel-agent:1", "rust-test-targets:ruxel-cli:1", "rust-test-targets:ruxel-core:1", "rust-workspace-members:5"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files.", "Rust build scripts are watched but their system dependencies and generated outputs are not inferred."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-docker.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 on providers [github-hosted]. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Local-provider jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "docker-tools-fixtures-docker"
label = "Docker (tools/fixtures/docker)"
kind = "docker"
root = "tools/fixtures/docker"
watch = ["tools/fixtures/docker/**"]
pr_commands = ["docker build --file 'tools/fixtures/docker/Dockerfile' --tag local-ci:tools-fixtures-docker-dockerfile 'tools/fixtures/docker'"]
full_commands = ["docker build --file 'tools/fixtures/docker/Dockerfile' --tag local-ci:tools-fixtures-docker-dockerfile 'tools/fixtures/docker'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = true
testcontainers = false
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false

[[unit]]
id = "rust-policy"
label = "Rust dependency policy"
kind = "rust"
root = "."
watch = [".cargo/audit.toml", ".cargo/deny.toml", "Cargo.lock", "Cargo.toml", "audit.toml", "deny.toml"]
pr_commands = ["mbx deny check"]
full_commands = ["mbx deny check"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "deny.toml"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-ruxel-agent"
label = "Rust crate (ruxel-agent)"
kind = "rust"
root = "crates/ruxel-agent"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/ruxel-agent/**/*.rs", "crates/ruxel-agent/Cargo.toml", "crates/ruxel-agent/benches/**", "crates/ruxel-agent/examples/**", "crates/ruxel-agent/src/**", "crates/ruxel-agent/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/ruxel-agent' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel-agent' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-agent' -- -D warnings", "cd -- 'crates/ruxel-agent' && mbx test --locked --all-features --package 'ruxel-agent'"]
full_commands = ["cd -- 'crates/ruxel-agent' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel-agent' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-agent' -- -D warnings", "cd -- 'crates/ruxel-agent' && mbx test --locked --all-features --package 'ruxel-agent'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/ruxel-agent' && mbx check --locked --profile test --all-targets --all-features --package 'ruxel-agent'"]
depends_on = ["rust-ruxel-core", "rust-ruxel-proto"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-ruxel-cli"
label = "Rust crate (ruxel-cli)"
kind = "rust"
root = "crates/ruxel"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/ruxel/**/*.rs", "crates/ruxel/Cargo.toml", "crates/ruxel/benches/**", "crates/ruxel/build.rs", "crates/ruxel/examples/**", "crates/ruxel/src/**", "crates/ruxel/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/ruxel' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-cli' -- -D warnings", "cd -- 'crates/ruxel' && mbx test --locked --all-features --package 'ruxel-cli'"]
full_commands = ["cd -- 'crates/ruxel' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-cli' -- -D warnings", "cd -- 'crates/ruxel' && mbx test --locked --all-features --package 'ruxel-cli'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/ruxel' && mbx check --locked --profile test --all-targets --all-features --package 'ruxel-cli'"]
depends_on = ["rust-ruxel-core", "rust-ruxel-proto"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-ruxel-core"
label = "Rust crate (ruxel-core)"
kind = "rust"
root = "crates/ruxel-core"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/ruxel-core/**/*.rs", "crates/ruxel-core/Cargo.toml", "crates/ruxel-core/benches/**", "crates/ruxel-core/examples/**", "crates/ruxel-core/src/**", "crates/ruxel-core/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/ruxel-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-core' -- -D warnings", "cd -- 'crates/ruxel-core' && mbx test --locked --all-features --package 'ruxel-core'"]
full_commands = ["cd -- 'crates/ruxel-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-core' -- -D warnings", "cd -- 'crates/ruxel-core' && mbx test --locked --all-features --package 'ruxel-core'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/ruxel-core' && mbx check --locked --profile test --all-targets --all-features --package 'ruxel-core'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-ruxel-proto"
label = "Rust crate (ruxel-proto)"
kind = "rust"
root = "crates/ruxel-proto"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/ruxel-proto/**/*.rs", "crates/ruxel-proto/Cargo.toml", "crates/ruxel-proto/benches/**", "crates/ruxel-proto/build.rs", "crates/ruxel-proto/examples/**", "crates/ruxel-proto/src/**", "crates/ruxel-proto/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/ruxel-proto' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel-proto' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-proto' -- -D warnings", "cd -- 'crates/ruxel-proto' && mbx test --locked --all-features --package 'ruxel-proto'"]
full_commands = ["cd -- 'crates/ruxel-proto' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/ruxel-proto' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-proto' -- -D warnings", "cd -- 'crates/ruxel-proto' && mbx test --locked --all-features --package 'ruxel-proto'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/ruxel-proto' && mbx check --locked --profile test --all-targets --all-features --package 'ruxel-proto'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-ruxel-spec-extract"
label = "Rust crate (ruxel-spec-extract)"
kind = "rust"
root = "tools/spec-extract"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "tools/spec-extract/**/*.rs", "tools/spec-extract/Cargo.toml", "tools/spec-extract/benches/**", "tools/spec-extract/examples/**", "tools/spec-extract/src/**", "tools/spec-extract/tests/**"]
pr_commands = ["cd -- 'tools/spec-extract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/spec-extract' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-spec-extract' -- -D warnings", "cd -- 'tools/spec-extract' && mbx test --locked --all-features --package 'ruxel-spec-extract'"]
full_commands = ["cd -- 'tools/spec-extract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/spec-extract' && mbx clippy --locked --profile test --all-targets --all-features --package 'ruxel-spec-extract' -- -D warnings", "cd -- 'tools/spec-extract' && mbx test --locked --all-features --package 'ruxel-spec-extract'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'tools/spec-extract' && mbx check --locked --profile test --all-targets --all-features --package 'ruxel-spec-extract'"]
depends_on = ["rust-ruxel-core"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
